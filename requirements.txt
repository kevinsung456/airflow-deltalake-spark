# ============================================================
# requirements.txt
# PIIDetectionOperator 의존 패키지 목록
#
# 설치 방법:
#   pip install -r requirements.txt
#
# presidio 엔진 사용 시 추가 설치:
#   pip install -r requirements.txt -r requirements-presidio.txt
# ============================================================

# ── Airflow ──────────────────────────────────────────────
apache-airflow>=2.7.0,<3.0.0

# ── Spark / Delta Lake ───────────────────────────────────
pyspark>=3.4.0,<4.0.0
delta-spark>=2.4.0,<4.0.0

# ── 데이터 처리 (pandas UDF 용) ──────────────────────────
pandas>=2.0.0,<3.0.0
pyarrow>=12.0.0          # pandas UDF Arrow 직렬화 필수

# ── 테스트 ───────────────────────────────────────────────
pytest>=7.4.0
pytest-mock>=3.11.0
pytest-timeout>=2.1.0    # 통합 테스트 타임아웃 제어
